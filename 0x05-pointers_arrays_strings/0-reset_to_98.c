#include "main.h"
/**
 * void reset_to_98(int *n);
 *a function that takes a pointer to an int as parameter and updates the value it points to to 98
 * */
void reset_to_98(int *n)
{
	/**
	 * change the number that n is ppinted them
	 */
	*n = 98;
}
